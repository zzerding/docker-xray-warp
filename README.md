## this is a docker run xray and docker run warp-cli example

### how to use this?
> change .env  and  config.json
> Install (Docker Engine)[https://docs.docker.com/engine/install/] or Docker Desktop  
> docker compose up -d 

